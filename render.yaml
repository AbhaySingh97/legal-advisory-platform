services:
  - type: web
    name: legal-advisory-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    context: backend
    plan: free
    region: singapore
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: DATABASE_NAME
        value: legal_advisory_db
      - key: ENVIRONMENT
        value: production
