services:
  - type: web
    name: legal-advisory-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    context: backend
    plan: free
    region: singapore
    envVars:
      - key: ENVIRONMENT
        value: production
